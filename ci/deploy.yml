---
platform: linux

image_resource:
  type: docker-image
  source:
     repository: teliaoss/concourse-python-aws
     tag: "latest"

inputs:
- name: build/distributions
- name: cloudwatch-lambda

run:
- path: deploy.sh
