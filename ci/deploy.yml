---
platform: linux

image_resource:
  type: docker-image
  source:
     repository: teliaoss/concourse-python-aws
     tag: "latest"

inputs:
- name: build/distributions
- name: dist

run:
 path: sh
 args: [./cloudwatch-lambda/ci/deploy.sh]
