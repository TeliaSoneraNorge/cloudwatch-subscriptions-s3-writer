---
platform: linux

image_resource:
  type: docker-image
  source:
     repository: teliaoss/concourse-awscli
     tag: "latest"

inputs:
- name: dist
- name: cloudwatch-lambda

run:
  path: sh
  args: [./cloudwatch-lambda/ci/deploy.sh]
